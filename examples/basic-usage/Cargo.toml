[package]
name = "basic-usage-example"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
wasm-bindgen = "0.2"

[profile.release]
# These will be automatically configured by wasm-slim
# but shown here for reference
# lto = "fat"
# opt-level = "s"
# strip = true
